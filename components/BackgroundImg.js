import classes from './BackgroundImg.module.css';

export default function BackgroundImg({imageUrl}) {

  return (
    // <div className={classes.imageContainer} style={{ BackgroundImg: `url(${imageUrl}), linear-gradient(to bottom, rgba(255, 255, 255, 0) 96%, rgba(66, 173, 248, 1) 100%)`}}>
    <div className={classes.imageContainer}>
    </div>
  )
}
